apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-cert
spec:
  secretName: test-cert-secret
  issuerRef:
    name: pebble-issuer
    kind: ClusterIssuer
  dnsNames:
  - test.example.local